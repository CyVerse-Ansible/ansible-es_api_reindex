galaxy_info:
  author: Jonathan Strootman
  description: Use the Elasticsearch reindex API from Ansible.
  company: CyVerse
  license: BSD
  min_ansible_version: 2.0
  #github_branch:
  platforms:
  - name: EL
    versions:
    - all
  - name: Fedora
    versions:
    - all
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  galaxy_tags: 
      - elasticsearch
      - api
      - reindex
    # NOTE: A tag is limited to a single word comprised of
    # alphanumeric characters. Maximum 20 tags per role.

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
